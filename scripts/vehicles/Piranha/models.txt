module Base
{
	model PZKA_Vehicles_Piranha_a
	{
		mesh = vehicles/pzka_Piranha|Piranha,
		shader = vehicle_multiuv_noreflect,
		invertX = FALSE,
		scale = 0.01,
	}

	model PZKA_Vehicles_Piranha_DoorFrontLeft
	{
		mesh = vehicles/pzka_Piranha|Door_FrontLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_Piranha_DoorFrontRight
	{
		mesh = vehicles/pzka_Piranha|Door_FrontRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightDoorBone 1.0,
	}

	model PZKA_Vehicles_Piranha_WindowFrontLeft
	{
		mesh = vehicles/pzka_Piranha|Window_FrontLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftWindowBone 1.0,

	}
	model PZKA_Vehicles_Piranha_WindowFrontRight
	{
		mesh = vehicles/pzka_Piranha|Window_FrontRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightWindowBone 1.0,
	}
	model PZKA_Vehicles_Piranha_Trunk
	{
		mesh = vehicles/pzka_Piranha|Trunk_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkBone 1.0,
	}
	model PZKA_Vehicles_Piranha_Hood
	{
		mesh = vehicles/pzka_Piranha|Hood_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = HoodBone 1.0,
	}
	model PZKA_Vehicles_Piranha_Windshield
	{
		mesh = vehicles/pzka_Piranha|Windshield_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
	model PZKA_Vehicles_Piranha_RearWindow
	{
		mesh = vehicles/pzka_Piranha|RearWindow_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
}