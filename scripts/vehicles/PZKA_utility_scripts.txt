module Base
{
	template vehicle PZKA_DoorFrontLeft {
		template! = PZKA_DoorFL,
		template! = PZKA_DoorFL_Fast,
	}
	template vehicle PZKA_DoorFrontRight {
		template! = PZKA_DoorFR,
		template! = PZKA_DoorFR_Fast,
	}	
	template vehicle PZKA_DoorMiddleLeft {
		template! = PZKA_DoorML,
		template! = PZKA_DoorML_Fast,
	}
	template vehicle PZKA_DoorMiddleRight {
		template! = PZKA_DoorMR,
		template! = PZKA_DoorMR_Fast,
	}	
	template vehicle PZKA_DoorRearLeft {
		template! = PZKA_DoorRL,
		template! = PZKA_DoorRL_Fast,
	}
	template vehicle PZKA_DoorRearRight {
		template! = PZKA_DoorRR,
		template! = PZKA_DoorRR_Fast,
	}
	
	template vehicle PZKA_DoorWindowFrontLeft {
		template! = PZKA_DoorFL,
		template! = PZKA_DoorFL_Reg,
		template! = PZKA_WindowFL,
	}
	template vehicle PZKA_DoorWindowFrontRight {
		template! = PZKA_DoorFR,
		template! = PZKA_DoorFR_Reg,
		template! = PZKA_WindowFR,
	}
	template vehicle PZKA_DoorWindowMiddleLeft {
		template! = PZKA_DoorML,
		template! = PZKA_DoorML_Reg,
		template! = PZKA_WindowML,
	}
	template vehicle PZKA_DoorWindowMiddleRight {
		template! = PZKA_DoorMR,
		template! = PZKA_DoorMR_Reg,
		template! = PZKA_WindowMR,
	}	
	template vehicle PZKA_DoorWindowRearLeft {
		template! = PZKA_DoorRL,
		template! = PZKA_DoorRL_Reg,
		template! = PZKA_WindowRL,
	}
	template vehicle PZKA_DoorWindowRearRight {
		template! = PZKA_DoorRR,
		template! = PZKA_DoorRR_Reg,
		template! = PZKA_WindowRR,
	}

	template vehicle PZKA_EngineDoor {
		template! = PZKA_Hood,
		template! = PZKA_Hood_Fast,
	}	
	
	
	template vehicle PZKA_ATA2AirScoopHoodFix
	{
		part ATA2AirScoop
		{			
			table install
			{
				complete = ATATuning2.InstallComplete.AirScoop,
			}
			table uninstall
			{
				complete = ATATuning2.UninstallComplete.AirScoop,
			}
		}
	}

	template vehicle pzka_LightHostAnimate {
		part PZKA_LightsHost
		{
			category = nodisplay,
            specificItem = false,
            itemType = Base.LightBulb,
            setAllModelsVisible = true,
			lua
	    	{
				create = Vehicles.Create.Default,
	    	}
			door
			{}		

			anim Close
			{
				anim = Lights_open,
				reverse = TRUE,
				rate = 1,
			}

			anim Open
			{
				anim = Lights_open,
				rate = 1,
			}

			anim Closed
			{
				anim = Lights_open,
				animate = FALSE,
			}

			anim Opened
			{
				anim = Lights_open,
				reverse = TRUE,
				animate = FALSE,
			}
		}
	}
	template vehicle PZKA_Interior {
		part Interior
		{
			category = nodisplay,
            specificItem = false,
            itemType = Base.LightBulb,
            setAllModelsVisible = true,
			lua
	    	{
			    create = Vehicles.Create.Default,
	    	}
		}
	}
	template vehicle PZKA_lightbar {
		part ATA2RoofLightbar
		{
			category = nodisplay,
            area = Engine,
            specificItem = false,
            itemType = Base.LightBulb,
            setAllModelsVisible = true,
			lua
	    	{
				create = Vehicles.Create.Default,
	    	}
		}
	}
	template vehicle PZKA_taxi {
		part ATA2RoofTaxiSign
		{
			category = nodisplay,
			specificItem = false,
			itemType = Base.LightBulb,
			lua
			{
				create = Vehicles.Create.Default,
			}
		}
	}
	template vehicle PZKA_Frontfill {
        part Frontfill
		{
			category = nodisplay,
            specificItem = false,
            itemType = Base.LightBulb,
            setAllModelsVisible = true,
			lua
	    	{
			    create = Vehicles.Create.Default,
	    	}
		}
    }
    template vehicle PZKA_Rearfill {
        part Rearfill
		{
			category = nodisplay,
            specificItem = false,
            itemType = Base.LightBulb,
            setAllModelsVisible = true,
			lua
	    	{
			    create = Vehicles.Create.Default,
	    	}
		}
    }
}