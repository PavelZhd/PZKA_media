module Base
{
	model PZKA_Vehicles_Champion_a
	{
		mesh = vehicles/pzka_Champion|Vehicles_Challenger,
		shader = vehicle_multiuv_noreflect,
		invertX = FALSE,
		scale = 0.01,
	}

	model PZKA_Vehicles_Champion_DoorFrontLeft
	{
		mesh = vehicles/pzka_Champion|Door_FrontLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_Champion_DoorFrontRight
	{
		mesh = vehicles/pzka_Champion|Door_FrontRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightDoorBone 1.0,
	}

	model PZKA_Vehicles_Champion_WindowFrontLeft
	{
		mesh = vehicles/pzka_Champion|Window_FrontLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftWindowBone 1.0,

	}
	model PZKA_Vehicles_Champion_WindowFrontRight
	{
		mesh = vehicles/pzka_Champion|Window_FrontRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightWindowBone 1.0,
	}
	model PZKA_Vehicles_Champion_Trunk
	{
		mesh = vehicles/pzka_Champion|Trunk_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkBone 1.0,
	}
	model PZKA_Vehicles_Champion_Hood
	{
		mesh = vehicles/pzka_Champion|Hood_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = HoodBone 1.0,
	}
	model PZKA_Vehicles_Champion_Windshield
	{
		mesh = vehicles/pzka_Champion|Windshield_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
	model PZKA_Vehicles_Champion_RearWindow
	{
		mesh = vehicles/pzka_Champion|RearWindow_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
}