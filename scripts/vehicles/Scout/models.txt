module Base
{
	model PZKA_Vehicles_Scout_a
	{
		mesh = vehicles/pzka_Scout|Scout,
		shader = vehicle_multiuv_noreflect,
		invertX = FALSE,
		scale = 0.01,
	}

	model PZKA_Vehicles_Scout_DoorFrontLeft
	{
		mesh = vehicles/pzka_Scout|Door_FrontLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_Scout_DoorFrontRight
	{
		mesh = vehicles/pzka_Scout|Door_FrontRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightDoorBone 1.0,
	}
	model PZKA_Vehicles_Scout_DoorRearLeft
	{
		mesh = vehicles/pzka_Scout|Door_RearLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_Scout_DoorRearRight
	{
		mesh = vehicles/pzka_Scout|Door_RearRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearRightDoorBone 1.0,
	}
	model PZKA_Vehicles_Scout_WindowFrontLeft
	{
		mesh = vehicles/pzka_Scout|Window_FrontLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftWindowBone 1.0,

	}
	model PZKA_Vehicles_Scout_WindowFrontRight
	{
		mesh = vehicles/pzka_Scout|Window_FrontRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightWindowBone 1.0,
	}
	model PZKA_Vehicles_Scout_WindowRearLeft
	{
		mesh = vehicles/pzka_Scout|Window_RearLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearLeftWindowBone 1.0,
	}
	model PZKA_Vehicles_Scout_WindowRearRight
	{
		mesh = vehicles/pzka_Scout|Window_RearRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearRightWindowBone 1.0,
	}
	model PZKA_Vehicles_Scout_Trunk
	{
		mesh = vehicles/pzka_Scout|Trunk_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkLBone 1.0,
		boneWeight = TrunkRBone 1.0,
	}
	model PZKA_Vehicles_Scout_Hood
	{
		mesh = vehicles/pzka_Scout|Hood_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = HoodBone 1.0,
	}
	model PZKA_Vehicles_Scout_Windshield
	{
		mesh = vehicles/pzka_Scout|Windshield_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
	model PZKA_Vehicles_Scout_RearWindow
	{
		mesh = vehicles/pzka_Scout|RearWindow_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
	}
	model PZKA_Vehicles_Scout_RearWindowStatic
	{
		mesh = vehicles/pzka_Scout|RearWindow_sobj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
}