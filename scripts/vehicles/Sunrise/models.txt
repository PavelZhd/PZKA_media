module Base
{
	model PZKA_Vehicles_Sunrise_a
	{
		mesh = vehicles/pzka_Sunrise|Sunrise,
		shader = vehicle_multiuv_noreflect,
		invertX = FALSE,
		scale = 0.01,
	}

	model PZKA_Vehicles_Sunrise_DoorFrontLeft
	{
		mesh = vehicles/pzka_Sunrise|Door_FrontLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_Sunrise_DoorFrontRight
	{
		mesh = vehicles/pzka_Sunrise|Door_FrontRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightDoorBone 1.0,
	}
	model PZKA_Vehicles_Sunrise_WindowFrontLeft
	{
		mesh = vehicles/pzka_Sunrise|Window_FrontLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftWindowBone 1.0,

	}
	model PZKA_Vehicles_Sunrise_WindowFrontRight
	{
		mesh = vehicles/pzka_Sunrise|Window_FrontRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightWindowBone 1.0,
	}
	model PZKA_Vehicles_Sunrise_WindowRearLeft
	{
		mesh = vehicles/pzka_Sunrise|Window_RearLeft_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
	model PZKA_Vehicles_Sunrise_WindowRearRight
	{
		mesh = vehicles/pzka_Sunrise|Window_RearRight_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
	model PZKA_Vehicles_Sunrise_Trunk
	{
		mesh = vehicles/pzka_Sunrise|Trunk_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkBone 1.0,
	}
	model PZKA_Vehicles_Sunrise_TrunkS
	{
		mesh = vehicles/pzka_Sunrise|Trunk_S_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkBone 1.0,
	}
	model PZKA_Vehicles_Sunrise_Hood
	{
		mesh = vehicles/pzka_Sunrise|Hood_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = HoodBone 1.0,
	}
	model PZKA_Vehicles_Sunrise_Windshield
	{
		mesh = vehicles/pzka_Sunrise|Windshield_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
	model PZKA_Vehicles_Sunrise_RearWindow
	{
		mesh = vehicles/pzka_Sunrise|RearWindow_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
}