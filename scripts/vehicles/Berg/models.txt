module Base
{
	model PZKA_Vehicles_Berg_a
	{
		mesh = vehicles/pzka_Berg|Berg,
		shader = vehicle_multiuv_noreflect,
		invertX = FALSE,
		scale = 0.01,
	}
	
	model PZKA_Vehicles_Berg_DoorFrontLeft
	{
		mesh = vehicles/pzka_Berg|Door_FrontLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_Berg_DoorFrontRight
	{
		mesh = vehicles/pzka_Berg|Door_FrontRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightDoorBone 1.0,
	}
	model PZKA_Vehicles_Berg_DoorRearLeft
	{
		mesh = vehicles/pzka_Berg|Door_RearLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_Berg_DoorRearRight
	{
		mesh = vehicles/pzka_Berg|Door_RearRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearRightDoorBone 1.0,
	}
	model PZKA_Vehicles_Berg_WindowFrontLeft
	{
		mesh = vehicles/pzka_Berg|Window_FrontLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftWindowBone 1.0,

	}
	model PZKA_Vehicles_Berg_WindowFrontRight
	{
		mesh = vehicles/pzka_Berg|Window_FrontRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightWindowBone 1.0,
	}
	model PZKA_Vehicles_Berg_WindowRearLeft
	{
		mesh = vehicles/pzka_Berg|Window_RearLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearLeftWindowBone 1.0,
	}
	model PZKA_Vehicles_Berg_WindowRearRight
	{
		mesh = vehicles/pzka_Berg|Window_RearRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearRightWindowBone 1.0,
	}
	model PZKA_Vehicles_Berg_Trunk
	{
		mesh = vehicles/pzka_Berg|Trunk_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkBone 1.0,
	}
	model PZKA_Vehicles_Berg_Hood
	{
		mesh = vehicles/pzka_Berg|Hood_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = HoodBone 1.0,
	}
	model PZKA_Vehicles_Berg_Windshield
	{
		mesh = vehicles/pzka_Berg|Windshield_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
	model PZKA_Vehicles_Berg_RearWindow
	{
		mesh = vehicles/pzka_Berg|RearWindow_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
	}
	model PZKA_Vehicles_Berg_RearWindow_static
	{
		mesh = vehicles/pzka_Berg|RearWindow_sobj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
}