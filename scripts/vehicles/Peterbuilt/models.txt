module Base
{
	model PZKA_Vehicles_Freight_S1_a
	{
		mesh = vehicles/pzka_Freight|Semi1,
		shader = vehicle_multiuv_noreflect,
		invertX = FALSE,
		scale = 0.01,
	}
	model PZKA_Vehicles_Freight_S1_nr_a
	{
		mesh = vehicles/pzka_Freight|Semi1,
		shader = vehicle_norandom_multiuv_noreflect,
		invertX = FALSE,
		scale = 0.01,
	}
	model PZKA_Vehicles_Freight_S2_a
	{
		mesh = vehicles/pzka_Freight|Semi2,
		shader = vehicle_multiuv_noreflect,
		invertX = FALSE,
		scale = 0.01,
	}
	model PZKA_Vehicles_Freight_T_nr_a
	{
		mesh = vehicles/pzka_Freight|Tow,
		shader = vehicle_norandom_multiuv_noreflect,
		invertX = FALSE,
		scale = 0.01,
	}

	model PZKA_Vehicles_Freight_DoorFrontLeft
	{
		mesh = vehicles/pzka_Freight|Door_FrontLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_Freight_DoorFrontRight
	{
		mesh = vehicles/pzka_Freight|Door_FrontRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightDoorBone 1.0,
	}
	model PZKA_Vehicles_Freight_WindowFrontLeft
	{
		mesh = vehicles/pzka_Freight|Window_FrontLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftWindowBone 1.0,

	}
	model PZKA_Vehicles_Freight_WindowFrontRight
	{
		mesh = vehicles/pzka_Freight|Window_FrontRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightWindowBone 1.0,
	}
	
	model PZKA_Vehicles_Freight_Hood
	{
		mesh = vehicles/pzka_Freight|Hood_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = HoodBone 1.0,
	}
	model PZKA_Vehicles_Freight_Windshield
	{
		mesh = vehicles/pzka_Freight|Windshield_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}

	model PZKA_Vehicles_Freight_DoorFrontLeft_nr
	{
		mesh = vehicles/pzka_Freight|Door_FrontLeft_obj,
		shader = vehicle_norandom_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_Freight_DoorFrontRight_nr
	{
		mesh = vehicles/pzka_Freight|Door_FrontRight_obj,
		shader = vehicle_norandom_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightDoorBone 1.0,
	}
	model PZKA_Vehicles_Freight_Hood_nr
	{
		mesh = vehicles/pzka_Freight|Hood_obj,
		shader = vehicle_norandom_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = HoodBone 1.0,
	}
}