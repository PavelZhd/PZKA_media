module Base
{
	model PZKA_Vehicles_HMMV_internals
	{
		mesh = vehicles/pzka_HMMV|Vehicles_Interior,
		shader = vehiclewheel,
		texture = Vehicles/Vehicle_pzkHMMVShell,
		static = TRUE,
		scale = 0.01,
	}
	model PZKA_Vehicles_HMMV_SeatFL
	{
		mesh = vehicles/pzka_HMMV|Seat_FrontLeft_obj,
		shader = vehiclewheel,
		texture = Vehicles/Vehicle_pzkHMMVShell,
		static = TRUE,
		scale = 0.01,
	}	
	model PZKA_Vehicles_HMMV_SeatFR
	{
		mesh = vehicles/pzka_HMMV|Seat_FrontRight_obj,
		shader = vehiclewheel,
		texture = Vehicles/Vehicle_pzkHMMVShell,
		static = TRUE,
		scale = 0.01,
	}
	model PZKA_Vehicles_HMMV_SeatRL
	{
		mesh = vehicles/pzka_HMMV|Seat_RearLeft_obj,
		shader = vehiclewheel,
		texture = Vehicles/Vehicle_pzkHMMVShell,
		static = TRUE,
		scale = 0.01,
	}
	model PZKA_Vehicles_HMMV_SeatRR
	{
		mesh = vehicles/pzka_HMMV|Seat_RearRight_obj,
		shader = vehiclewheel,
		texture = Vehicles/Vehicle_pzkHMMVShell,
		static = TRUE,
		scale = 0.01,
	}
	model PZKA_HMMV_Battery
	{
		mesh = vehicles/pzka_HMMV|Battery_obj,
		shader = vehiclewheel,
		texture = WorldItems/CarBattery,
		static = TRUE,
		scale = 0.01,
	}
}