module Base
{
	model PZKA_Vehicles_Bunny_a
	{
		mesh = vehicles/pzka_Bunny|Bunny,
		shader = vehicle_multiuv_noreflect,
		invertX = FALSE,
		scale = 0.01,
	}

	model PZKA_Vehicles_Bunny_DoorFrontLeft
	{
		mesh = vehicles/pzka_Bunny|Door_FrontLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_Bunny_DoorFrontRight
	{
		mesh = vehicles/pzka_Bunny|Door_FrontRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightDoorBone 1.0,
	}

	model PZKA_Vehicles_Bunny_WindowFrontLeft
	{
		mesh = vehicles/pzka_Bunny|Window_FrontLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftWindowBone 1.0,

	}
	model PZKA_Vehicles_Bunny_WindowFrontRight
	{
		mesh = vehicles/pzka_Bunny|Window_FrontRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightWindowBone 1.0,
	}
	model PZKA_Vehicles_Bunny_WindowRearLeft
	{
		mesh = vehicles/pzka_Bunny|Window_RearLeft_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
	model PZKA_Vehicles_Bunny_WindowRearRight
	{
		mesh = vehicles/pzka_Bunny|Window_RearRight_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
	model PZKA_Vehicles_Bunny_Trunk
	{
		mesh = vehicles/pzka_Bunny|Trunk_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkBone 1.0,
	}
	model PZKA_Vehicles_Bunny_Hood
	{
		mesh = vehicles/pzka_Bunny|Hood_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = HoodBone 1.0,
	}
	model PZKA_Vehicles_Bunny_Windshield
	{
		mesh = vehicles/pzka_Bunny|Windshield_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
	model PZKA_Vehicles_Bunny_RearWindow
	{
		mesh = vehicles/pzka_Bunny|RearWindow_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkBone 1.0,
	}
}