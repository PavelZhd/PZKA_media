module Base
{
	model PZKA_Vehicles_DashNoble_LightFL
	{
		mesh = vehicles/pzka_DashNoble|Light_FrontLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = LightsBone 1.0,
	}	
	model PZKA_Vehicles_DashNoble_LightFR
	{
		mesh = vehicles/pzka_DashNoble|Light_FrontRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = LightsBone 1.0,
	}
	model PZKA_Vehicles_DashNoble_LightFL_static
	{
		mesh = vehicles/pzka_DashNoble|Light_FrontLeft_sobj,
		shader = vehicle_multiuv_vva,
		static = TRUE,
		scale = 0.01,
	}	
	model PZKA_Vehicles_DashNoble_LightFR_static
	{
		mesh = vehicles/pzka_DashNoble|Light_FrontRight_sobj,
		shader = vehicle_multiuv_vva,
		static = TRUE,
		scale = 0.01,
	}
	model PZKA_Vehicles_DashNoble_LightRL
	{
		mesh = vehicles/pzka_DashNoble|Light_RearLeft_obj,
		shader = vehicle_multiuv_vva,
		static = TRUE,
		scale = 0.01,
	}
	model PZKA_Vehicles_DashNoble_LightRR
	{
		mesh = vehicles/pzka_DashNoble|Light_RearRight_obj,
		shader = vehicle_multiuv_vva,
		static = TRUE,
		scale = 0.01,
	}


	template vehicle pzka_DashNoble_Light {
		part HeadlightLeft 
		{
			parent = LightsHost,
			model Default
			{
				file = PZKA_Vehicles_DashNoble_LightFL,
			}
			model Static
			{
				file = PZKA_Vehicles_DashNoble_LightFL_static,
			}
		}
		part HeadlightRight 
		{
			parent = LightsHost,
			model Default
			{
				file = PZKA_Vehicles_DashNoble_LightFR,
			}
			model Static
			{
				file = PZKA_Vehicles_DashNoble_LightFR_static,
			}
		}
		part HeadlightRearLeft 
		{
			model Default
			{
				file = PZKA_Vehicles_DashNoble_LightRL,
			}
		}
		part HeadlightRearRight 
		{
			model Default
			{
				file = PZKA_Vehicles_DashNoble_LightRR,
			}
		}
	}
}