module Base
{
	model PZKA_Vehicles_Islander_a
	{
		mesh = vehicles/pzka_Islander|Vehicles_Capri,
		shader = vehicle_multiuv_noreflect,
		invertX = FALSE,
		scale = 0.01,
	}

	model PZKA_Vehicles_Islander_DoorFrontLeft
	{
		mesh = vehicles/pzka_Islander|Door_FrontLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_Islander_DoorFrontRight
	{
		mesh = vehicles/pzka_Islander|Door_FrontRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightDoorBone 1.0,
	}	
	model PZKA_Vehicles_Islander_WindowFrontLeft
	{
		mesh = vehicles/pzka_Islander|Window_FrontLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftWindowBone 1.0,

	}
	model PZKA_Vehicles_Islander_WindowFrontRight
	{
		mesh = vehicles/pzka_Islander|Window_FrontRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightWindowBone 1.0,
	}
	
	model PZKA_Vehicles_Islander_Trunk
	{
		mesh = vehicles/pzka_Islander|Trunk_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkBone 1.0,
	}
	model PZKA_Vehicles_Islander_Hood
	{
		mesh = vehicles/pzka_Islander|Hood_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = HoodBone 1.0,
	}
	model PZKA_Vehicles_Islander_Windshield
	{
		mesh = vehicles/pzka_Islander|Windshield_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
	model PZKA_Vehicles_Islander_RearWindow
	{
		mesh = vehicles/pzka_Islander|RearWindow_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
}