module Base
{
	model PZKA_Vehicles_Bug_LightFL
	{
		mesh = vehicles/pzka_Bug|Light_FrontLeft_obj,
		shader = vehicle_multiuv_vva,
		static = TRUE,
		scale = 0.01,
	}	
	model PZKA_Vehicles_Bug_LightFR
	{
		mesh = vehicles/pzka_Bug|Light_FrontRight_obj,
		shader = vehicle_multiuv_vva,
		static = TRUE,
		scale = 0.01,
	}
	model PZKA_Vehicles_Bug_LightRL
	{
		mesh = vehicles/pzka_Bug|Light_RearLeft_obj,
		shader = vehicle_multiuv_vva,
		static = TRUE,
		scale = 0.01,
	}
	model PZKA_Vehicles_Bug_LightRR
	{
		mesh = vehicles/pzka_Bug|Light_RearRight_obj,
		shader = vehicle_multiuv_vva,
		static = TRUE,
		scale = 0.01,
	}

	template vehicle pzka_Bug_Light {
		part HeadlightLeft 
		{
			model Default
			{
				file = PZKA_Vehicles_Bug_LightFL,
			}
			area = TruckBed,
		}
		part HeadlightRight 
		{
			model Default
			{
				file = PZKA_Vehicles_Bug_LightFR,
			}
			area = TruckBed,
		}
		part HeadlightRearLeft 
		{
			model Default
			{
				file = PZKA_Vehicles_Bug_LightRL,
			}
			area = Engine,
		}
		part HeadlightRearRight 
		{
			model Default
			{
				file = PZKA_Vehicles_Bug_LightRR,
			}
			area = Engine,
		}
	}
}