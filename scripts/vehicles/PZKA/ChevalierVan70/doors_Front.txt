module Base
{
	model PZKA_Vehicles_CVan70_DoorFrontLeft
	{
		mesh = vehicles/pzka_CVan70|Door_FrontLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_CVan70_DoorFrontRight
	{
		mesh = vehicles/pzka_CVan70|Door_FrontRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightDoorBone 1.0,
	}
	model PZKA_Vehicles_CVan70_DoorFrontLeft_nr
	{
		mesh = vehicles/pzka_CVan70|Door_FrontLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_CVan70_DoorFrontRight_nr
	{
		mesh = vehicles/pzka_CVan70|Door_FrontRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightDoorBone 1.0,
	}

	model PZKA_Vehicles_CVan70_WindowFrontLeft
	{
		mesh = vehicles/pzka_CVan70|Window_FrontLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftWindowBone 1.0,

	}
	model PZKA_Vehicles_CVan70_WindowFrontRight
	{
		mesh = vehicles/pzka_CVan70|Window_FrontRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightWindowBone 1.0,
	}

	template vehicle PZKA_ChevalierVan70_FrontDoorWindows {
		part WindowFrontLeft
        {
			parent = DoorFrontLeft,
			model Default
        	{
            	file = PZKA_Vehicles_CVan70_WindowFrontLeft,
            }            		
        }
		part WindowFrontRight
    	{	
            parent = DoorFrontRight,
			model Default
    		{
        		file = PZKA_Vehicles_CVan70_WindowFrontRight,
    		}
	    }
	}
	template vehicle PZKA_CVan70_FrontDoor {
		part DoorFrontLeft
		{
			model Default
			{
				file = PZKA_Vehicles_CVan70_DoorFrontLeft,
			}
		}
		part DoorFrontRight
		{
			model Default
			{
				file = PZKA_Vehicles_CVan70_DoorFrontRight,
			}
		}
		template! = PZKA_ChevalierVan70_FrontDoorWindows,
		template! = PZKA_DoorWindowFrontLeft,
		template! = PZKA_DoorWindowFrontRight,
	}
	template vehicle PZKA_CVan70_FrontDoor_nr {
		part DoorFrontLeft
		{
			model Default
			{
				file = PZKA_Vehicles_CVan70_DoorFrontLeft_nr,
			}
		}
		part DoorFrontRight
		{
			model Default
			{
				file = PZKA_Vehicles_CVan70_DoorFrontRight_nr,
			}
		}
		template! = PZKA_ChevalierVan70_FrontDoorWindows,
		template! = PZKA_DoorWindowFrontLeft,
		template! = PZKA_DoorWindowFrontRight,
	}
}