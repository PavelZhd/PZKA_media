module Base
{
	model PZKA_Vehicles_SWagon_DoorRearRight
	{
		mesh = vehicles/Normal/pzka_Wagon_RRdoor|Door_RearRight_Wagon_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearRightDoorBone 1.0,
	}
	model PZKA_Vehicles_SWagon_WindowRearRight
	{
		mesh = vehicles/Normal/pzka_Wagon_RRdoor|Window_RearRight_Wagon_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearRightWindowBone 1.0,
	}
	model PZKA_Vehicles_SWagon_DoorRearRight_nr
	{
		mesh = vehicles/Normal/pzka_Wagon_RRdoor|Door_RearRight_Wagon_obj,
		shader = vehicle_norandom_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearRightDoorBone 1.0,
	}

	template vehicle PZKA_SWagon_RearDoorWindows {
		part WindowRearLeft
        {
			parent = DoorRearLeft,
			model Default
            {
            	file = VVA_Vehicles_SWagon_WindowRearLeft,
            }            		
        }
		part WindowRearRight
    	{	
        	parent = DoorRearRight,
			model Default
            {
                file = PZKA_Vehicles_SWagon_WindowRearRight,
            }
	    }
	}

	template vehicle PZKA_SWagon_RearDoors {
		part DoorRearLeft
		{
			model Default
			{
				file = VVA_Vehicles_SWagon_DoorRearLeft,
			}
		}
		template! = VVA_DoorWindowRearLeft,

		part DoorRearRight
		{
			model Default
			{
				file = PZKA_Vehicles_SWagon_DoorRearRight,
			}
		}
		template! = VVA_DoorWindowRearRight,
		template! = PZKA_SWagon_RearDoorWindows,
	}
	template vehicle PZKA_SWagon_RearDoors_nr {
		part DoorRearLeft
		{
			model Default
			{
				file = VVA_Vehicles_SWagon_DoorRearLeft_nr,
			}
		}
		template! = VVA_DoorWindowRearLeft,

		part DoorRearRight
		{
			model Default
			{
				file = PZKA_Vehicles_SWagon_DoorRearRight_nr,
			}
		}
		template! = VVA_DoorWindowRearRight,
		template! = PZKA_SWagon_RearDoorWindows,
	}
}