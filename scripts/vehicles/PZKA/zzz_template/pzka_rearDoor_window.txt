module Base
{
    model PZKA_Vehicles_LABEL_RearWindow
	{
		mesh = vehicles/[FILENAME]|RearWindow_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
	}
	model PZKA_Vehicles_LABEL_RearWindow_static
	{
		mesh = vehicles/[FILENAME]|RearWindow_sobj,
		shader = vehicle_multiuv_vva,
		static = TRUE,
		scale = 0.01,
	}
	model PZKA_Vehicles_LABEL_Trunk
	{
		mesh = vehicles/[FILENAME]|Trunk_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkLeftBone 1.0,
		boneWeight = TrunkRightBone 1.0,
	}
	model PZKA_Vehicles_LABEL_Trunk_nr
	{
		mesh = vehicles/[FILENAME]|Trunk_obj,
		shader = vehicle_norandom_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkLeftBone 1.0,
		boneWeight = TrunkRightBone 1.0,
	}
	template vehicle PZKA_LABEL_RearWindow {
		part WindshieldRear
		{
			parent = DoorRear,
			model Default
			{
				file = PZKA_Vehicles_LABEL_RearWindow,
			}
			model Static
			{
				file = PZKA_Vehicles_LABEL_RearWindow_static,
			}
		}
	}
	template vehicle PZKA_LABEL_Trunk {
		part DoorRear
		{
			model Default
			{
				file = PZKA_Vehicles_LABEL_Trunk,
			}
		}
		template! = PZKA_LABEL_RearWindow,
		template! = VVA_RearDoorVersionMed,
	}
	template vehicle PZKA_LABEL_Trunk_nr {
		part DoorRear
		{
			model Default
			{
				file = PZKA_Vehicles_LABEL_Trunk_nr,
			}
		}
		template! = PZKA_LABEL_RearWindow,
		template! = VVA_RearDoorVersionMed,
	}
}