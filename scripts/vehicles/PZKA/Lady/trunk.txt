module Base
{
	model PZKA_Vehicles_Lady_Trunk
	{
		mesh = vehicles/pzka_Lady|Trunk_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkBone 1.0,
	}
	model PZKA_Vehicles_Lady_Trunk_nr
	{
		mesh = vehicles/pzka_Lady|Trunk_obj,
		shader = vehicle_norandom_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkBone 1.0,
	}
	model PZKA_Vehicles_Lady_RearWindow
	{
		mesh = vehicles/pzka_Lady|RearWindow_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkBone 1.0,
	}

	template vehicle PZKA_Lady_RearWindow {
		part WindshieldRear
		{
			parent = TrunkDoor,
			model Default
			{
				file = PZKA_Vehicles_Lady_RearWindow,
			}
		}
	}
	template vehicle PZKA_Lady_Trunk {
		part TrunkDoor
		{
			model Default
			{
				file = PZKA_Vehicles_Lady_Trunk,
			}		
		}
		template! = PZKA_Lady_RearWindow,
		template! = VVA_TrunkDoorWindowVersion,
	}
	template vehicle PZKA_Lady_Trunk {
		part TrunkDoor
		{
			model Default
			{
				file = PZKA_Vehicles_Lady_Trunk,
			}		
		}
		template! = PZKA_Lady_RearWindow,
		template! = VVA_TrunkDoorWindowVersion,
	}
}