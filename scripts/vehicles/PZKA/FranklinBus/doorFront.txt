module Base
{
	model PZKA_Vehicles_TruckBus_DoorFrontRight
	{
		mesh = vehicles/pzka_TruckBus|Door_FrontRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = DoorFrontRightOneBone 1.0,
		boneWeight = DoorFrontRightTwoBone 1.0,
	}
	model PZKA_Vehicles_TruckBus_DoorFrontRight_nr	
	{
		mesh = vehicles/pzka_TruckBus|Door_FrontRight_obj,
		shader = vehicle_norandom_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = DoorFrontRightOneBone 1.0,
		boneWeight = DoorFrontRightTwoBone 1.0,
	}
	model PZKA_Vehicles_TruckBus_WindowFrontLeft
	{
		mesh = vehicles/pzka_TruckBus|Window_FrontLeft_obj,
		shader = vehicle_multiuv_vva,
		static = TRUE,
		scale = 0.01,

	}
	model PZKA_Vehicles_TruckBus_WindowFrontRight
	{
		mesh = vehicles/pzka_TruckBus|Window_FrontRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
	}
	template vehicle PZKA_FranklinBus_FrontWindows {		
		part WindowFrontLeft
        	{
			model Default
            		{
                		file = PZKA_Vehicles_TruckBus_WindowFrontLeft,
            		}            		
        	}
		part WindowFrontRight
        {	
            parent = DoorFrontRight,
			model Default
        	{
          		file = PZKA_Vehicles_TruckBus_WindowFrontRight,
        	}
	    }		
	}
	template vehicle PZKA_FranklinBus_FrontDoors { 
		part DoorFrontRight
		{
			model Default
			{
				file = PZKA_Vehicles_TruckBus_DoorFrontRight,
			}
		}
		template!= PZKA_FranklinBus_FrontWindows,
		template! = PZKA_DoorFR,
		template! = PZKA_DoorFR_Reg,
	}
	
	template vehicle PZKA_FranklinBus_FrontDoors_nr { 
		part DoorFrontRight
		{
			model Default
			{
				file = PZKA_Vehicles_TruckBus_DoorFrontRight,
			}
		}
		template!= PZKA_FranklinBus_FrontWindows,
		template! = PZKA_DoorFR,
		template! = PZKA_DoorFR_Reg,
	}	
}