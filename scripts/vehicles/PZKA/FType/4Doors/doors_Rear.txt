module Base
{
	model VVA_Vehicles_FType_DoorRearLeft
	{
		mesh = vehicles/FType/pzka_4Door|Door_RearLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearLeftDoorBone 1.0,
	}
	model VVA_Vehicles_FType_DoorRearRight
	{
		mesh = vehicles/FType/pzka_4Door|Door_RearRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearRightDoorBone 1.0,
	}
	model VVA_Vehicles_FType_WindowRearLeft
	{
		mesh = vehicles/FType/pzka_4Door|Window_RearLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearLeftWindowBone 1.0,
	}
	model VVA_Vehicles_FType_WindowRearRight
	{
		mesh = vehicles/FType/pzka_4Door|Window_RearRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearRightWindowBone 1.0,
	}
	model VVA_Vehicles_FType_DoorRearLeft_nr
	{
		mesh = vehicles/FType/pzka_4Door|Door_RearLeft_obj,
		shader = vehicle_norandom_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearLeftDoorBone 1.0,
	}
	model VVA_Vehicles_FType_DoorRearRight_nr
	{
		mesh = vehicles/FType/pzka_4Door|Door_RearRight_obj,
		shader = vehicle_norandom_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearRightDoorBone 1.0,
	}

	template vehicle PZKA_FType_RearDoorWindows {
		part WindowRearLeft
        {
			parent = DoorRearLeft,
			model Default
            {
            	file = VVA_Vehicles_FType_WindowRearLeft,
            }            		
        }
		part WindowRearRight
    	{	
        	parent = DoorRearRight,
			model Default
            {
                file = VVA_Vehicles_FType_WindowRearRight,
            }
	    }
	}

	template vehicle PZKA_FType_RearDoors {
		part DoorRearLeft
		{
			model Default
			{
				file = VVA_Vehicles_FType_DoorRearLeft,
			}
		}
		template! = PZKA_DoorWindowRearLeft,

		part DoorRearRight
		{
			model Default
			{
				file = VVA_Vehicles_FType_DoorRearRight,
			}
		}
		template! = PZKA_DoorWindowRearRight,
		template! = PZKA_FType_RearDoorWindows,
	}
	template vehicle PZKA_FType_RearDoors_nr {
		part DoorRearLeft
		{
			model Default
			{
				file = VVA_Vehicles_FType_DoorRearLeft_nr,
			}
		}
		template! = PZKA_DoorWindowRearLeft,

		part DoorRearRight
		{
			model Default
			{
				file = VVA_Vehicles_FType_DoorRearRight_nr,
			}
		}
		template! = PZKA_DoorWindowRearRight,
		template! = PZKA_FType_RearDoorWindows,
	}
}