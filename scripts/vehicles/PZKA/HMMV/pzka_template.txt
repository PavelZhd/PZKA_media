module Base
{
	template vehicle PZKA_HMMV_shared {
		template! = PZKA_HMMV_Windshield,
		template! = PZKA_HMMV_FrontLights,
		template! = PZKA_HMMV_RearLights,
		template! = PZKA_HMMV_Interior,
		part Battery 
		{
			model Default 
			{
				file = PZKA_HMMV_Battery,
			}
		}
	}
	
	template vehicle PZKA_HMMV {
		template! = PZKA_HMMV_hood,
		template! = PZKA_HMMV_shared,
	}
	template vehicle PZKA_HMMV_nr {
		template! = PZKA_HMMV_hood_nr,
		template! = PZKA_HMMV_shared,
	}
}