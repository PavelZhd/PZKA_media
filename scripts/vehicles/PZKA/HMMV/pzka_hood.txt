module Base
{
	model PZKA_Vehicles_HMMV_Hood
	{
		mesh = vehicles/HMMV/pzka_HMMV|Hood_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = HoodBone 1.0,
		boneWeight = BullBarBone 1.0,
	}
	model PZKA_Vehicles_HMMV_Hood_nr
	{
		mesh = vehicles/HMMV/pzka_HMMV|Hood_obj,
		shader = vehicle_norandom_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = HoodBone 1.0,
		boneWeight = BullBarBone 1.0,
	}
	model PZKA_Vehicles_HMMV_Bullbad_nr
	{
		mesh = vehicles/HMMV/pzka_HMMV|Bullbar_obj,
		shader = vehicle_norandom_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
	}
	template vehicle PZKA_HMMV_Bullbar {
		part Bullbar
		{
			parent = EngineDoor,
			model Default
			{
				file = PZKA_Vehicles_HMMV_Bullbad_nr,
			}
			category = nodisplay,
            specificItem = false,
            itemType = Base.LightBulb,
            setAllModelsVisible = true,	
			lua
	    	{
			    create = Vehicles.Create.Default,
	    	}
		}
		
	}
	
	template vehicle PZKA_HMMV_hood {
		part EngineDoor
		{
			model Default
			{
				file = PZKA_Vehicles_HMMV_Hood,
			}
		}
		template! = VVA_EngineDoorHeadlights,
		template! = PZKA_HMMV_Bullbar,
	}	
	template vehicle PZKA_HMMV_hood_nr {
		part EngineDoor
		{
			model Default
			{
				file = PZKA_Vehicles_HMMV_Hood_nr,
			}
		}
		template! = VVA_EngineDoorHeadlights,
		template! = PZKA_HMMV_Bullbar,
	}
}