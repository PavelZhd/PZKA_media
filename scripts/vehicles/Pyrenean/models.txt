module Base
{
	model PZKA_Vehicles_Pyre_a
	{
		mesh = vehicles/pzka_Pyre|Vehicles_SportsCar,
		shader = vehicle_multiuv_noreflect,
		invertX = FALSE,
		scale = 0.01,
	}

	model PZKA_Vehicles_Pyre_DoorFrontLeft
	{
		mesh = vehicles/pzka_Pyre|Door_FrontLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_Pyre_DoorFrontRight
	{
		mesh = vehicles/pzka_Pyre|Door_FrontRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightDoorBone 1.0,
	}
	
	model PZKA_Vehicles_Pyre_WindowFrontLeft
	{
		mesh = vehicles/pzka_Pyre|Window_FrontLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftWindowBone 1.0,
		boneWeight = FrontLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_Pyre_WindowFrontRight
	{
		mesh = vehicles/pzka_Pyre|Window_FrontRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightWindowBone 1.0,
		boneWeight = FrontRightDoorBone 1.0,
	}
	model PZKA_Vehicles_Pyre_Trunk
	{
		mesh = vehicles/pzka_Pyre|Trunk_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkBone 1.0,
	}
	model PZKA_Vehicles_Pyre_Hood
	{
		mesh = vehicles/pzka_Pyre|Hood_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = HoodBone 1.0,
	}
	model PZKA_Vehicles_Pyre_Windshield
	{
		mesh = vehicles/pzka_Pyre|Windshield_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
	model PZKA_Vehicles_Pyre_RearWindow
	{
		mesh = vehicles/pzka_Pyre|RearWindow_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkBone 1.0,
	}
	model PZKA_Vehicles_Pyre_RearWindow_static
	{
		mesh = vehicles/pzka_Pyre|RearWindow_sobj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
	
}