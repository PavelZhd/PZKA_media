module Base
{
	model PZKA_Vehicles_GTA_a
	{
		mesh = vehicles/pzka_GTA|Vehicles_GTO,
		shader = vehicle_multiuv_noreflect,
		invertX = FALSE,
		scale = 0.01,
	}

	model PZKA_Vehicles_GTA_DoorFrontLeft
	{
		mesh = vehicles/pzka_GTA|Door_FrontLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_GTA_DoorFrontRight
	{
		mesh = vehicles/pzka_GTA|Door_FrontRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightDoorBone 1.0,
	}	
	model PZKA_Vehicles_GTA_WindowFrontLeft
	{
		mesh = vehicles/pzka_GTA|Window_FrontLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftWindowBone 1.0,

	}
	model PZKA_Vehicles_GTA_WindowFrontRight
	{
		mesh = vehicles/pzka_GTA|Window_FrontRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightWindowBone 1.0,
	}
	model PZKA_Vehicles_GTA_Trunk
	{
		mesh = vehicles/pzka_GTA|Trunk_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkBone 1.0,
	}
	model PZKA_Vehicles_GTA_Hood
	{
		mesh = vehicles/pzka_GTA|Hood_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = HoodBone 1.0,
	}
	model PZKA_Vehicles_GTA_Windshield
	{
		mesh = vehicles/pzka_GTA|Windshield_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
	model PZKA_Vehicles_GTA_RearWindow
	{
		mesh = vehicles/pzka_GTA|RearWindow_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
}