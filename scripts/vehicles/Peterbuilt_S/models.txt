module Base
{
	model PZKA_Vehicles_PeterbuiltS_a
	{
		mesh = vehicles/pzka_Peterbuilt_Sleeper|PeterbuildLong,
		shader = vehicle_multiuv_noreflect,
		invertX = FALSE,
		scale = 0.01,
	}
	model PZKA_Vehicles_PeterbuiltS_nr_a
	{
		mesh = vehicles/pzka_Peterbuilt_Sleeper|PeterbuildLong,
		shader = vehicle_norandom_multiuv_noreflect,
		invertX = FALSE,
		scale = 0.01,
	}

	model PZKA_Vehicles_PeterbuiltS_DoorFrontLeft
	{
		mesh = vehicles/pzka_Peterbuilt_Sleeper|Door_FrontLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_PeterbuiltS_DoorFrontRight
	{
		mesh = vehicles/pzka_Peterbuilt_Sleeper|Door_FrontRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightDoorBone 1.0,
	}
	model PZKA_Vehicles_PeterbuiltS_WindowFrontLeft
	{
		mesh = vehicles/pzka_Peterbuilt_Sleeper|Window_FrontLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftWindowBone 1.0,

	}
	model PZKA_Vehicles_PeterbuiltS_WindowFrontRight
	{
		mesh = vehicles/pzka_Peterbuilt_Sleeper|Window_FrontRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightWindowBone 1.0,
	}
	
	model PZKA_Vehicles_PeterbuiltS_Hood
	{
		mesh = vehicles/pzka_Peterbuilt_Sleeper|Hood_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = HoodBone 1.0,
	}
	model PZKA_Vehicles_PeterbuiltS_Windshield
	{
		mesh = vehicles/pzka_Peterbuilt_Sleeper|Windshield_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}

	model PZKA_Vehicles_PeterbuiltS_DoorFrontLeft_nr
	{
		mesh = vehicles/pzka_Peterbuilt_Sleeper|Door_FrontLeft_obj,
		shader = vehicle_norandom_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_PeterbuiltS_DoorFrontRight_nr
	{
		mesh = vehicles/pzka_Peterbuilt_Sleeper|Door_FrontRight_obj,
		shader = vehicle_norandom_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightDoorBone 1.0,
	}
	model PZKA_Vehicles_PeterbuiltS_Hood_nr
	{
		mesh = vehicles/pzka_Peterbuilt_Sleeper|Hood_obj,
		shader = vehicle_norandom_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = HoodBone 1.0,
	}
}