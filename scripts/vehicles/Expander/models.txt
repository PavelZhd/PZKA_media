module Base
{
	model PZKA_Vehicles_Expander_a
	{
		mesh = vehicles/pzka_Expander|Expander,
		shader = vehicle_multiuv_noreflect,
		invertX = FALSE,
		scale = 0.01,
	}

	model PZKA_Vehicles_Expander_DoorFrontLeft
	{
		mesh = vehicles/pzka_Expander|Door_FrontLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_Expander_DoorFrontRight
	{
		mesh = vehicles/pzka_Expander|Door_FrontRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightDoorBone 1.0,
	}
	model PZKA_Vehicles_Expander_DoorRearLeft
	{
		mesh = vehicles/pzka_Expander|Door_RearLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearLeftDoorBone 1.0,
	}
	model PZKA_Vehicles_Expander_DoorRearRight
	{
		mesh = vehicles/pzka_Expander|Door_RearRight_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearRightDoorBone 1.0,
	}
	model PZKA_Vehicles_Expander_WindowFrontLeft
	{
		mesh = vehicles/pzka_Expander|Window_FrontLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontLeftWindowBone 1.0,

	}
	model PZKA_Vehicles_Expander_WindowFrontRight
	{
		mesh = vehicles/pzka_Expander|Window_FrontRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontRightWindowBone 1.0,
	}
	model PZKA_Vehicles_Expander_WindowRearLeft
	{
		mesh = vehicles/pzka_Expander|Window_RearLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearLeftWindowBone 1.0,
	}
	model PZKA_Vehicles_Expander_WindowRearRight
	{
		mesh = vehicles/pzka_Expander|Window_RearRight_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = RearRightWindowBone 1.0,
	}
	model PZKA_Vehicles_Expander_TrunkS
	{
		mesh = vehicles/pzka_Expander|TrunkS_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkBone 1.0,
	}
	model PZKA_Vehicles_Expander_Trunk
	{
		mesh = vehicles/pzka_Expander|Trunk_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = TrunkBone 1.0,
	}
	model PZKA_Vehicles_Expander_Hood
	{
		mesh = vehicles/pzka_Expander|Hood_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = HoodBone 1.0,
	}
	model PZKA_Vehicles_Expander_Windshield
	{
		mesh = vehicles/pzka_Expander|Windshield_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
	model PZKA_Vehicles_Expander_RearWindow
	{
		mesh = vehicles/pzka_Expander|RearWindow_obj,
		shader = vehicle_multiuv_vva,
		scale = 0.01,
	}
}