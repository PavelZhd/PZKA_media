module Base
{
	model VVA_Vehicles_PZK_HMMV_DoorFrontLeft
	{
		mesh = vehicles/PZKA/HMMV/doorF|Door_FrontLeft_obj,
		shader = vehicle_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontDoor.L 1.0,
	}
	model VVA_Vehicles_PZK_HMMV_DoorFrontLeft_nr
	{
		mesh = vehicles/PZKA/HMMV/doorF|Door_FrontLeft_obj,
		shader = vehicle_norandom_multiuv_noreflect,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontDoor.L 1.0,
	}
	model VVA_Vehicles_PZK_HMMV_WindowFrontLeft
	{
		mesh = vehicles/PZKA/HMMV/doorF|Window_FrontLeft_obj,
		shader = vehicle_multiuv_vva,
		static = FALSE,
		scale = 0.01,
		boneWeight = FrontWindow.L 1.0,
	}

	template vehicle VVA_PZK_HMMV_FrontLeftShared {
		part WindowFrontLeft
        {
			model Default
            {
            	file = VVA_Vehicles_PZK_HMMV_WindowFrontLeft,
            }
		}
		template! = VVA_WindowFL,
		template! = VVA_DoorFL_Common,
	}

	template vehicle VVA_PZK_HMMV_FrontLeft {
		part DoorFrontLeft
		{
			model Default
			{
				file = VVA_Vehicles_PZK_HMMV_DoorFrontLeft,
			}
		}
		template! = VVA_PZK_HMMV_FrontLeftShared,
	}

	template vehicle VVA_PZK_HMMV_FrontLeft_nr {
		part DoorFrontLeft
		{
			model Default
			{
				file = VVA_Vehicles_PZK_HMMV_DoorFrontLeft_nr,
			}
		}
		template! = VVA_PZK_HMMV_FrontLeftShared,
	}
}
